/**
 * Shared domain / DTO types for Creative Marketing.
 * Prefer Zod-inferred types from `@/lib/validation` when available.
 */

export type ServiceCategoryCode = "THINK" | "CREATE" | "BUILD" | "GROW";
